experiment:
  name: __EXP_NAME__
  description: cg without preconditioner

output:
  results_dir: results/__EXP_NAME__
  save_residual_history: true

execution:
  command: >
    julia --project scripts/run_solver.jl
    --config experiments/__EXP_NAME__/config.yaml

postprocess:
  commands: []

# Optional: external data reference (lightweight v0.1)
# external_data:
#   - name: materials_db
#     type: database        # database | api | dataset | filesystem
#     locator: "material_id=1234"   # query / ID / URL / path / DOI
#     version: "v2.1"       # optional
#     retrieved_at: "2026-03-03T10:12:00+09:00"  # optional
#     note: "optional memo"

run:
